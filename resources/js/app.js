// Livewire 3 comes with AlpineJS pre-bundled and auto-injects it.
// Initializing Alpine here causes a "multiple instances" conflict,
// which breaks Livewire's form submissions.
// import Alpine from 'alpinejs';
// window.Alpine = Alpine;
// Alpine.start();
